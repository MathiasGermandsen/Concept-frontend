# Backend URL for Nginx reverse proxy (used at runtime, not build time)
# In Dokploy: set this to your backend service URL, e.g. http://backend:8080
BACKEND_URL=http://localhost:8080

# API key for authenticating with the backend (baked into JS at build time)
VITE_API_KEY=
